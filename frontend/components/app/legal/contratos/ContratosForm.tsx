'use client'

// ContratosForm — formulario del submódulo Contratos

export default function ContratosForm({ onClose, onSaved }: { onClose: () => void; onSaved: () => void }) {
  return (
    <div className="text-slate-400 text-sm">Formulario de Contratos</div>
  )
}
